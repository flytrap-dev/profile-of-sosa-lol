# Flytrap

Personal site. C++ on Windows internals.

```bash
npm install
npm run dev
```

Content lives in `src/data/site.ts`. Leave `experience` and `archive` empty to hide those blocks.

```bash
npm run build
npm run preview
```

GitHub Pages deploys from `main` via Actions.
